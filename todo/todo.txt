1. Tabella Corsi:
--> certificati deve puntare a titoliCorsi?
--> da gestire con referenza titoliCorsi:xx o lasciamo con intero?

2. Certificato/corso
numero min/max discenti
durata corso in ore
durata corso in lezioni
faculty
rapporto docenti/affiancamento

3. sparsi 25 lug 2015
X aggiungere numero di partecipanti al corso (deve limitare il numero di istruttori)
- verificare la modifica di tutte le sezioni del corso
- nei form di creazione inserire selettore dell'orario del corso!!!
- nel form inserimento insegnanti dare errore se inserito un numero di insegnanti inferiore al dovuto
- elenco corsi, se presidente o delegato del presidente, deve mostrare anche corsi "pendenti" per poterli modificare, oppure ci deve essere un calendario/elenco corsi solo per corsi pendenti
- elenco corsi deve mostrare anche corsi "pendenti" se chi cerca è nel faculty (vedi api_miei_corsi)
- formazione.corsi.crea e tutto il flusso di conseguenza dovrebbe diventare admin.corsi.crea
- abilitare uso di paginaCorso() nelle pagine di creazione del corso
- prevedere CSRF ?
- creazione corsi: valutare se inserire il try-catch in una funzione da qualche parte
- verificare che api_istruttori_in_affiancamento_cerca in ApiServer ritorni solo gli istruttori in affiancamento
- funzione ( o entità nuova? ) per la verifica che un volontario sia effettivamente un istruttore
- sistemare i selettori per essere indipendenti tra loro (vedi selettore istruttori VS selettore istruttori in affiancamento)
- usare throw new Errore al posto di throw new Exception (brrrrr) per poter codificare numericamente gli errori
- gestire caso in cui il presidente/delegato escluda dal corso un insegnante che si era già confermato (funzione Partecipazione->ritira())
- formazione.corsi.istruttori.ok: aggiungere verifica del fatto che il volontario aggiunto sia effettivamente un istruttore


--------------------------------

DA CHIEDERE: 

elenco istruttori

elenco istruttori in affiancamento

elenco formatori

se la partecipazione di un istruttore è ritirata
prima che partano i giorni "buffer", c'è ancora tempo per cercare altri istruttori
diversamente il corso viene annullato d'ufficio (funzione Partecipazione->ritira())

quando un corso è ancora cancellabile dal proprio creatore?
0 iscritti?

--------------------------------

Aggiornare database:

ALTER TABLE `corsi` ADD COLUMN `partecipanti` INT NULL DEFAULT NULL COMMENT '' AFTER `progressivo`;
ALTER TABLE `corsi` ADD COLUMN `iscritti` INT(11) NULL COMMENT '' AFTER `descrizione`;
ALTER TABLE `corsi` CHANGE COLUMN `referente` `responsabile` INT(11) NULL DEFAULT NULL COMMENT '' ;


ALTER TABLE `certificati` ADD COLUMN `discenti_per_insegnante` INT NOT NULL COMMENT '' AFTER `nome`;
ALTER TABLE `certificati` ADD COLUMN `tipo_insegnanti` VARCHAR(45) NOT NULL COMMENT '' AFTER `discenti_per_insegnante`;
ALTER TABLE `certificati` ADD COLUMN `tipo_discenti` VARCHAR(45) NOT NULL COMMENT '' AFTER `insegnanti`;

UPDATE `certificati` SET `tipo_insegnanti`='istruttori', `tipo_discenti`='popolazione' WHERE `id`='1';
UPDATE `certificati` SET `tipo_insegnanti`='istruttori', `tipo_discenti`='volontari' WHERE `id`='4';
UPDATE `certificati` SET `nome`='FORMAZIONE ISTRUTTORI', `tipo_insegnanti`='formatori', `tipo_discenti`='istruttori' WHERE `id`='3';
UPDATE `certificati` SET `tipo_insegnanti`='istruttori', `tipo_discenti`='volontari' WHERE `id`='2';


ALTER TABLE `partecipazioniCorsi` RENAME TO  `partecipazioni_corsi` ;

--------------------------------

Maaaaaaaaaaaaaaaa

e se volessi un costruttore particolare per una classe derivata da Entità?


